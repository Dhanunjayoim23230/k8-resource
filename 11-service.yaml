kind: Service
apiVersion: v1
metadata:
  name: internal-service
  namespace: prod
spec:
  selector:
    course: devops
    trainer: dhanunjaya
    application: backend
    version: clusterIP
  ports:
  - name: name-of-service-port
    protocol: "TCP"
    port: 80
    targetPort: 8080


---
kind: Pod
apiVersion: v1
metadata:
  name: servicepod
  namespace: prod
  labels:
    course: devops
    trainer: dhanunjaya
    application: backend
    version: clusterIP
spec:
  containers:
  - name: nginx
    image: nginx