kind: ConfigMap
apiVersion: v1
metadata:
  name: configmap
  namespace: prod
data:
  course: devops
  practice: mac


---
kind: Pod
apiVersion: v1
metadata:
  name: configpod
  namespace: prod
  
spec:
  containers:
    - name: nginx
      image: nginx
      labels: 
      envFrom:
        - configMapRef:
            name: configmap