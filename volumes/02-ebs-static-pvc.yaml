kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-ebs-claim

spec:
    mounts:
      mountPath: /usr/share/nginx/html
    resources:
      requests:
        storage: "25Gi"
    volumeName:
      name: pv-ebs-static